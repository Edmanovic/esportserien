# Sync Engine

Offline-first encrypted vault synchronization.

Planned responsibilities:

- Opaque encrypted item revision tracking.
- Conflict detection.
- Client-side conflict resolution.
- WebSocket sync protocol.
- Encrypted local cache coordination.

The sync engine can route ciphertext and metadata; it cannot require backend plaintext access.

