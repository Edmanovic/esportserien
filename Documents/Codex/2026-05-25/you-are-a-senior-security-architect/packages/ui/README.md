# UI Package

Shared React UI primitives for ESPASS desktop and admin surfaces.

Security notes:

- No secret persistence.
- No unsafe HTML rendering.
- Secret display components must support masking, reveal timers, and clipboard auto-clear hooks.

