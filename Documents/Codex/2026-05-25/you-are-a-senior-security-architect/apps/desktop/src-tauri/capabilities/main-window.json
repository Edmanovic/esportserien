{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal ESPASS desktop permissions. Vault and IPC commands are added explicitly after security review.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}

